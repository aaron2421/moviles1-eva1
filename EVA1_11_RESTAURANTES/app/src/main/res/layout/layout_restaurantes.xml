<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/imagenView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        app:srcCompat="@drawable/f1"
        tools:layout_editor_absoluteX="0dp" />

    <TextView
        android:id="@+id/txtViewMensaje"
        android:layout_width="231dp"
        android:layout_height="68dp"
        android:gravity="center"
        android:text="¡VIAJES!"
        android:textColor="#FAF7F7"
        android:textSize="48sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/imagenView"
        app:layout_constraintEnd_toEndOf="@+id/imagenView"
        app:layout_constraintStart_toStartOf="@+id/imagenView"
        app:layout_constraintTop_toTopOf="@+id/imagenView" />

</androidx.constraintlayout.widget.ConstraintLayout>